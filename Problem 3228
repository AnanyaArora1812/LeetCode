class Solution(object):
    def maxOperations(self, s):
        ops = 0
        ones = 0
        
        for i in range(len(s)):
            if s[i] == '1':
                ones += 1
            elif i > 0 and s[i-1] == '1':
                ops += ones
        
        return ops
